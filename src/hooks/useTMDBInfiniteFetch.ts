import { MediaSingleItemData } from "../utils/media-parser";

type Props = {
  mediaData: MediaSingleItemData[];
  queryData: any; // TODO
};

const useTMDBInfiniteFetch = ({ mediaData, queryData }: Props) => {
  // TODO
};

export default useTMDBInfiniteFetch;
